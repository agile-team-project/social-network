<h1> Add a new group thingy here and stuff </h1>

<%= simple_form_for @group, url: groups_path do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <br />
  <%= f.submit "Create Group", class: 'btn btn-primary' %>
  <br />
<% end %>